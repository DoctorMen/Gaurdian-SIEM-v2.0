# Guardian SIEM v2.0 — Dependencies

# Core
flask>=3.0
flask-sock>=0.7.0
pyyaml>=6.0
requests>=2.31

# Network monitoring
scapy>=2.5

# GeoIP (optional — falls back to ip-api.com)
geoip2>=4.7

# PDF Reports (optional — falls back to HTML reports)
# reportlab>=4.0

# YARA scanning (optional)
# yara-python>=4.3

# Password hashing (optional — falls back to SHA-256)
# bcrypt>=4.0

# Windows Event Log (Windows only, optional)
# pywin32>=306

# Testing
pytest>=7.0
